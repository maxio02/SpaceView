import 'package:flutter/material.dart';

class FullscreenArticleScreen extends StatelessWidget {
  const FullscreenArticleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
